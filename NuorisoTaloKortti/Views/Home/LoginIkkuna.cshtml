@model NuorisoTaloKortti.Models.Kayttajat
@{
    ViewBag.Title = "Login";
}


<center>
    @using (Html.BeginForm("Authorize", "Home", FormMethod.Post, new { id = "loginForm" }))
    {
        @Html.AntiForgeryToken()
        <h2>Kirjaudu Sisään</h2>
        <div>
            <div class="row col-md-3">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(model => model.KayttajaId)
                <div style="font-weight: bolder; font-size: larger; color: blue">
                    <h5>Käyttäjänimi</h5>
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Kayttajanimi, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Kayttajanimi, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div style="font-weight: bolder; font-size: larger; color: blue">
                    <h5>Salasana</h5>
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Salasana, new { htmlAttributes = new { @class = "form-control", @type = "password" } })
                        @Html.ValidationMessageFor(model => model.Salasana, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
            <label class="text-danger">

                @Html.DisplayFor(model => model.LoginErrorMessage)

            </label>
        </div>

        <button type="submit" class="btn btn-success" form="loginForm" style="text-align: center">Kirjaudu</button>

        <div class="col-md-offset-2 col-md-10"></div>

    }
</center>